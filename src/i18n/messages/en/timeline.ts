/**
 * Timeline copy.
 *
 * Timeline never names a period by assembling translated fragments. Period
 * labels come from `format.ts`, which asks the platform for the locale's own
 * date wording.
 */
export const timelineMessages = {
  'timeline.page.title': 'Timeline',
} as const
